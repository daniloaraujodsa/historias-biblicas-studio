# Dados locais (não versionados)

Pastas geradas em runtime:

- `projects/` — áudio, imagens e metadados por projeto
- `exports/` — MP4, thumbnails e ZIPs YouTube
- `music/` — trilhas opcionais
- `studio.db` — SQLite

Esses caminhos estão no `.gitignore`. Rode o app uma vez para criá-los.
